# SkillHub PRD v0.2

> 📋 Updated: 2026-02-08 | 경쟁 분석 및 차별화 전략 반영

---

## 1. 개요

### 1.1 배경

AI 에이전트 스킬 시장이 빠르게 성장하고 있다:
- Claude Code, Codex, ChatGPT 등 에이전트 환경 확산
- 스킬 수요 증가 → 마켓플레이스 등장
- **Skillstore.io** 등 선발 주자가 이미 3,000+ 스킬 보유

**문제점:**
- 스킬이 많아질수록 "좋은 스킬" 찾기 어려움
- 설치해서 써봐야 품질을 알 수 있음
- 비개발자에게 선택 기준 부재

### 1.2 경쟁 환경

| 서비스 | 스킬 수 | 강점 | 약점 |
|--------|---------|------|------|
| **Skillstore.io** | 3,175+ | 원클릭 설치, 보안 등급, 멀티 플랫폼 | 설치 전 품질 판단 어려움 |
| **GitHub 직접** | ∞ | 자유로움 | 발견 어려움, 품질 보장 X |
| **SkillHub (우리)** | 0 → | **AI 사전 분석** | 후발 주자 |

### 1.3 비전

> **"설치 전에 AI가 검증하는 스킬 마켓플레이스"**

기존: 검색 → 설치 → 사용 → 판단
**SkillHub**: 검색 → **AI 분석** → 추천 → 설치

### 1.4 핵심 차별화: Frontmatter 기반 AI 분석

```
┌─────────────────────────────────────────────────────┐
│  사용자: "코드 리뷰 스킬 찾아줘"                      │
├─────────────────────────────────────────────────────┤
│  SkillHub AI:                                       │
│  1. 후보 스킬 10개 수집                              │
│  2. Frontmatter 분석 (품질 점수 산출)                │
│     - 필수 필드 완성도                               │
│     - Description 구체성                            │
│     - Dependencies 명확성                           │
│     - Runtime 요구사항 적절성                        │
│  3. 상위 3개 추천 + 분석 근거 제시                   │
├─────────────────────────────────────────────────────┤
│  "code-reviewer (95점) 추천합니다.                   │
│   이유: 명확한 use-case, 낮은 토큰 사용량,           │
│   sonnet 모델로 충분, 의존성 없음"                   │
└─────────────────────────────────────────────────────┘
```

### 1.5 목표

**Phase 1 (MVP):**
- Frontmatter 품질 점수 시스템
- AI 기반 스킬 추천
- 기본 마켓플레이스 UI

**Phase 2:**
- 대화형 스킬 탐색
- 사용자 피드백 반영 추천

**Phase 3:**
- 토너먼트/랭킹
- 커뮤니티 기능

### 1.6 비목표 (MVP 제외)
- 경제 레이어 (수익 모델)
- 모바일 앱
- 스킬 직접 실행 (설치 가이드만 제공)

---

## 2. 타겟 사용자

### 2.1 Primary: AI 도구 사용자 (비개발자 포함)

| 페르소나 | 니즈 | 고민 |
|----------|------|------|
| **기획자 민수** | PRD 작성 스킬 | "어떤 스킬이 좋은지 모르겠어" |
| **디자이너 지영** | Figma 자동화 | "설치했는데 안 맞으면 시간 낭비" |
| **QA 철수** | 테스트케이스 생성 | "비슷한 스킬이 10개인데 뭘 써야 해?" |
| **주니어 개발자 영희** | 코드 리뷰 | "토큰 많이 쓰는 스킬은 피하고 싶어" |

### 2.2 Secondary: 스킬 제작자

- 자신의 스킬 등록/홍보
- 품질 점수로 개선점 파악
- 경쟁 스킬 대비 포지셔닝

---

## 3. 핵심 기능

### 3.1 Frontmatter 품질 분석 시스템

**점수 산출 기준 (100점 만점):**

| 항목 | 배점 | 평가 기준 |
|------|------|----------|
| **Core 필드 완성도** | 25점 | name, description, version, tags, author |
| **Description 품질** | 20점 | 길이, 구체성, 문제-해결 구조 |
| **Runtime 명세** | 15점 | 모델 요구사항, 필요 도구 명시 |
| **Resources 힌트** | 15점 | 토큰 예측, 컨텍스트 힌트 |
| **Use-cases 명확성** | 15점 | 구체적 사용 시나리오 |
| **Dependencies 관리** | 10점 | 의존성 명시 및 버전 관리 |

**등급 체계:**

| 등급 | 점수 | 뱃지 |
|------|------|------|
| S | 90-100 | ⭐ Premium |
| A | 80-89 | ✅ Verified |
| B | 70-79 | 📋 Standard |
| C | 60-69 | ⚠️ Basic |
| D | 0-59 | ❌ Incomplete |

### 3.2 AI 스킬 추천 엔진

**입력:**
- 사용자 쿼리 (자연어)
- 필터 조건 (카테고리, 모델, 토큰 제한 등)

**처리:**
```
1. 쿼리 분석 → 의도 파악
2. 후보 스킬 검색 (태그, 설명 매칭)
3. Frontmatter 분석 (품질 점수)
4. 맥락 기반 랭킹
5. 추천 + 근거 생성
```

**출력:**
- 추천 스킬 목록 (점수순)
- 각 스킬별 추천 이유
- 비교표 (토큰, 모델, 의존성)

### 3.3 스킬 마켓플레이스

#### 3.3.1 홈페이지
```
┌─────────────────────────────────────────────────────┐
│  🔍 검색바 + AI 추천 버튼                            │
│  "어떤 스킬을 찾으시나요?"                           │
├─────────────────────────────────────────────────────┤
│  📊 Featured Skills (S등급)                         │
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐                       │
│  │⭐  │ │⭐  │ │⭐  │ │⭐  │                       │
│  └────┘ └────┘ └────┘ └────┘                       │
├─────────────────────────────────────────────────────┤
│  🏷️ 카테고리별                                      │
│  [기획] [디자인] [개발] [QA] [문서화] [DevOps]       │
├─────────────────────────────────────────────────────┤
│  📈 Trending / ⭐ Top Rated / 🆕 New                │
└─────────────────────────────────────────────────────┘
```

#### 3.3.2 스킬 카드
```
┌─────────────────────────────────┐
│  🔍 code-reviewer          ⭐ S │
│  ─────────────────────────────  │
│  PR과 코드 변경사항을 리뷰하고   │
│  개선점을 제안하는 스킬          │
│  ─────────────────────────────  │
│  📊 95점 | 🤖 sonnet | 📦 1.8k  │
│  👤 woosgem | ⬇️ 1.2k installs  │
│  ─────────────────────────────  │
│  [상세보기]  [AI 분석]  [설치]   │
└─────────────────────────────────┘
```

#### 3.3.3 스킬 상세 페이지
```
┌─────────────────────────────────────────────────────┐
│  🔍 code-reviewer                              ⭐ S  │
│  by woosgem | v1.2.0 | MIT                          │
├─────────────────────────────────────────────────────┤
│  📊 AI 품질 분석                                    │
│  ┌───────────────────────────────────────────────┐  │
│  │ Core 필드: ████████████████████ 25/25        │  │
│  │ Description: ████████████████── 18/20        │  │
│  │ Runtime: ████████████████████── 14/15        │  │
│  │ Resources: ██████████████────── 12/15        │  │
│  │ Use-cases: ████████████████████ 15/15        │  │
│  │ Dependencies: ████████████████── 11/10       │  │
│  │ ─────────────────────────────────────────    │  │
│  │ 총점: 95/100 (S등급)                          │  │
│  └───────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────┤
│  📋 Description                                     │
│  PR과 코드 변경사항을 리뷰하고 개선점을 제안...       │
├─────────────────────────────────────────────────────┤
│  🎯 Use Cases                                       │
│  • PR 코드 리뷰                                     │
│  • 레거시 코드 분석                                  │
│  • 보안 취약점 검토                                  │
├─────────────────────────────────────────────────────┤
│  ⚙️ Requirements                                    │
│  • Model: sonnet 이상                               │
│  • Tools: Read, Glob, Grep                          │
│  • Est. Tokens: ~1,800                              │
├─────────────────────────────────────────────────────┤
│  📦 Dependencies                                    │
│  • markdown-formatter >=1.0.0                       │
├─────────────────────────────────────────────────────┤
│  [📋 설치 가이드]  [🔗 GitHub]  [🐛 이슈 리포트]     │
└─────────────────────────────────────────────────────┘
```

#### 3.3.4 AI 추천 모달 (⌘K)
```
┌─────────────────────────────────────────────────────┐
│  🤖 AI 스킬 추천                               [X]  │
├─────────────────────────────────────────────────────┤
│  💬 "코드 리뷰 도와줄 스킬 찾아줘.                   │
│      토큰은 적게 쓰면 좋겠어"                        │
├─────────────────────────────────────────────────────┤
│  🔍 분석 중... (3개 후보 검토)                      │
├─────────────────────────────────────────────────────┤
│  ✨ 추천 결과:                                      │
│                                                     │
│  1. code-reviewer (95점) ⭐ 최고 추천               │
│     → 토큰 1.8k로 효율적, sonnet OK                 │
│                                                     │
│  2. pr-analyzer (87점)                              │
│     → 기능 풍부하나 토큰 3.2k 소모                  │
│                                                     │
│  3. quick-review (82점)                             │
│     → 가장 가벼움(800 토큰), 기능 제한적            │
├─────────────────────────────────────────────────────┤
│  💡 "토큰 효율을 원하시니 code-reviewer를            │
│      추천드립니다. 필수 기능 모두 포함하면서         │
│      토큰 사용량이 적절합니다."                      │
└─────────────────────────────────────────────────────┘
```

### 3.4 스킬 등록

#### 유저 스토리
```
As a 스킬 제작자
I want GitHub repo URL로 스킬을 등록하고 싶다
So that 다른 사람들이 내 스킬을 발견하고 AI 분석을 받을 수 있다

수용 기준:
- [ ] GitHub URL 입력으로 스킬 등록
- [ ] Frontmatter 자동 파싱
- [ ] 품질 점수 즉시 산출
- [ ] 개선 제안 제공 (점수 올리는 방법)
- [ ] 버전 업데이트 자동 감지 (webhook)
```

### 3.5 비교 기능

```
┌─────────────────────────────────────────────────────┐
│  📊 스킬 비교                                       │
├─────────────────────────────────────────────────────┤
│              │ code-reviewer │ pr-analyzer │ quick │
├──────────────┼───────────────┼─────────────┼───────┤
│ 품질 점수     │ 95 ⭐         │ 87 ✅       │ 82 ✅ │
│ 토큰 예상     │ 1,800        │ 3,200       │ 800   │
│ 최소 모델     │ sonnet       │ sonnet      │ haiku │
│ 의존성        │ 1개          │ 0개         │ 0개   │
│ Use-cases    │ 3개          │ 5개         │ 1개   │
│ 업데이트      │ 3일 전       │ 2주 전      │ 1달 전│
└─────────────────────────────────────────────────────┘
```

---

## 4. 데이터 모델

### 4.1 핵심 엔티티

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   Author    │────<│    Skill    │>────│   Review    │
└─────────────┘     └─────────────┘     └─────────────┘
                           │
                    ┌──────┴──────┐
                    │             │
              ┌─────────┐   ┌─────────┐
              │ Quality │   │  Stats  │
              │  Score  │   │         │
              └─────────┘   └─────────┘
```

### 4.2 스키마 (Prisma)

```prisma
model Author {
  id          String   @id @default(cuid())
  githubId    String   @unique
  username    String   @unique
  avatarUrl   String?
  skills      Skill[]
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model Skill {
  id          String   @id @default(cuid())
  
  // Core (Layer 1)
  name        String
  description String   @db.Text
  version     String
  tags        String[]
  
  // Author
  authorId    String
  author      Author   @relation(fields: [authorId], references: [id])
  
  // Location (Layer 2)
  sourceType  String   @default("github")
  sourceOwner String
  sourceRepo  String
  sourcePath  String
  sourceRef   String   @default("main")
  
  // Runtime (Layer 2)
  minModel    String?
  requiredTools String[]
  optionalTools String[]
  baseTokens  Int?
  contextHint String?
  
  // Extended (Layer 3)
  useCases    String[]
  targetRoles String[]
  icon        String?
  license     String?
  status      SkillStatus @default(ACTIVE)
  
  // Quality Score
  qualityScore    QualityScore?
  
  // Stats
  installCount    Int      @default(0)
  
  // Relations
  reviews     Review[]
  
  // Timestamps
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  @@unique([authorId, name])
  @@index([tags])
  @@index([status])
}

model QualityScore {
  id              String @id @default(cuid())
  skillId         String @unique
  skill           Skill  @relation(fields: [skillId], references: [id])
  
  // 항목별 점수
  coreFieldScore      Int  // /25
  descriptionScore    Int  // /20
  runtimeScore        Int  // /15
  resourcesScore      Int  // /15
  useCasesScore       Int  // /15
  dependenciesScore   Int  // /10
  
  // 총점 및 등급
  totalScore      Int      // /100
  grade           Grade
  
  // AI 분석 코멘트
  analysisComment String?  @db.Text
  improvementTips String[] // 점수 올리는 팁
  
  // 분석 시점
  analyzedAt      DateTime @default(now())
}

model Review {
  id        String   @id @default(cuid())
  skillId   String
  skill     Skill    @relation(fields: [skillId], references: [id])
  
  rating    Int      // 1-5
  comment   String?  @db.Text
  
  // 익명 or GitHub 연동
  authorName String?
  authorGithubId String?
  
  createdAt DateTime @default(now())
}

enum SkillStatus {
  DRAFT
  ACTIVE
  DEPRECATED
  ARCHIVED
}

enum Grade {
  S
  A
  B
  C
  D
}
```

---

## 5. API 설계

### 5.1 스킬 API

| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/api/skills` | 스킬 목록 (필터, 정렬, 페이징) |
| GET | `/api/skills/:author/:name` | 스킬 상세 |
| POST | `/api/skills` | 스킬 등록 (GitHub URL) |
| PUT | `/api/skills/:id` | 스킬 업데이트 |
| DELETE | `/api/skills/:id` | 스킬 삭제 |

### 5.2 추천 API

| Method | Endpoint | 설명 |
|--------|----------|------|
| POST | `/api/recommend` | AI 스킬 추천 |
| GET | `/api/recommend/featured` | Featured 스킬 |
| GET | `/api/recommend/trending` | Trending 스킬 |

**POST /api/recommend:**
```json
{
  "query": "코드 리뷰 도와줄 스킬",
  "filters": {
    "maxTokens": 2000,
    "minGrade": "B",
    "model": "sonnet"
  },
  "limit": 5
}
```

**Response:**
```json
{
  "recommendations": [
    {
      "skill": { ... },
      "score": 95,
      "matchReason": "토큰 효율적, 요구 모델 적합",
      "rank": 1
    }
  ],
  "summary": "토큰 효율을 원하시니 code-reviewer를 추천드립니다."
}
```

### 5.3 분석 API

| Method | Endpoint | 설명 |
|--------|----------|------|
| POST | `/api/analyze` | Frontmatter 분석 (등록 전 미리보기) |
| GET | `/api/skills/:id/analysis` | 저장된 분석 결과 |

---

## 6. 기술 스택

| 영역 | 선택 | 이유 |
|------|------|------|
| Frontend | Next.js 14 + TypeScript | App Router, SSR, SEO |
| UI | woosgem DS (@woosgem-dev/react) | 자체 디자인 시스템 |
| Styling | Tailwind CSS | 유틸리티, DS와 조합 |
| Backend | Next.js API Routes | 풀스택 단일 배포 |
| Database | PostgreSQL (Railway) | 관계형, 안정적 |
| ORM | Prisma | 타입 안전, 마이그레이션 |
| AI | Claude API (Anthropic) | 추천 엔진 |
| Auth | GitHub OAuth | 스킬 제작자 인증 |
| Deploy | Vercel | Next.js 최적화 |
| Monitoring | Vercel Analytics | 기본 제공 |

---

## 7. 필요 컴포넌트 (DS 업데이트)

### 7.1 신규 필요

| 컴포넌트 | 용도 | 우선순위 |
|----------|------|----------|
| `ScoreBar` | 품질 점수 시각화 (막대 그래프) | 🔴 높음 |
| `GradeBadge` | S/A/B/C/D 등급 뱃지 | 🔴 높음 |
| `SkillCard` | 스킬 카드 (리스트용) | 🔴 높음 |
| `CompareTable` | 스킬 비교표 | 🟡 중간 |
| `CommandPalette` | ⌘K 모달 (AI 추천) | 🟡 중간 |
| `Chat` | AI 대화 UI | 🟢 낮음 (Phase 2) |

### 7.2 기존 활용

| 컴포넌트 | 용도 |
|----------|------|
| `Button` | 설치, 분석 등 액션 |
| `Input` | 검색, 필터 |
| `Modal` | 상세 보기, 비교 |
| `Tag` | 태그 표시 |
| `Avatar` | 작성자 프로필 |

---

## 8. 마일스톤

| Phase | 목표 | 기간 | 산출물 |
|-------|------|------|--------|
| **Phase 1** | 기반 + 분석 시스템 | 2주 | DB, API, 품질 점수 |
| **Phase 2** | 마켓플레이스 UI | 2주 | 홈, 목록, 상세 |
| **Phase 3** | AI 추천 | 1주 | 추천 API, ⌘K 모달 |
| **Phase 4** | 스킬 등록 | 1주 | 등록 플로우, GitHub 연동 |
| **Phase 5** | 폴리싱 + 런칭 | 1주 | 최적화, 배포 |

**예상 총 기간: 7주**

---

## 9. 성공 지표

| 지표 | 목표 (런칭 후 1개월) |
|------|----------------------|
| 등록 스킬 수 | 100+ |
| AI 추천 요청 | 500+/월 |
| 추천 만족도 (피드백) | 80%+ |
| 재방문율 | 30%+ |

---

## 10. 리스크 및 대응

| 리스크 | 대응 |
|--------|------|
| Skillstore가 유사 기능 추가 | 품질 분석 깊이로 차별화, 한국 시장 집중 |
| AI 분석 비용 | 캐싱, 분석 주기 최적화 |
| 스킬 등록 저조 | 직접 큐레이션, 인기 스킬 먼저 등록 |

---

## 11. 다음 단계

1. [x] PRD v0.2 작성 (본 문서)
2. [ ] DS 컴포넌트 설계 (ScoreBar, GradeBadge, SkillCard)
3. [ ] Prisma 스키마 확정 및 마이그레이션
4. [ ] 품질 점수 알고리즘 구현
5. [ ] API 엔드포인트 구현

---

*작성: Thrall ⚡*
*검토: WooSGem*
*버전: v0.2 (2026-02-08)*
