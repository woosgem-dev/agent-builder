# SkillHub MVP Architecture

> 2026-02-07 | Woosgem + Thrall

---

## 시스템 개요

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    Next.js App                          │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐              │   │
│  │  │  Store   │  │  Detail  │  │  Search  │              │   │
│  │  │  (List)  │  │  (Skill) │  │          │              │   │
│  │  └──────────┘  └──────────┘  └──────────┘              │   │
│  │                                                         │   │
│  │  ┌─────────────────────────────────────────────────┐   │   │
│  │  │           @woosgem/ds-react                     │   │   │
│  │  └─────────────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                          SERVER                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                 Next.js API Routes                      │   │
│  │                                                         │   │
│  │  /api/skills          → 스킬 목록/검색                   │   │
│  │  /api/skills/[id]     → 스킬 상세                       │   │
│  │  /api/skills/register → 스킬 등록 (GitHub URL)          │   │
│  │  /api/rankings        → 랭킹 조회                       │   │
│  │  /api/auth/*          → GitHub OAuth                    │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                  │
│                              ▼                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    Services                             │   │
│  │                                                         │   │
│  │  ┌────────────┐ ┌────────────┐ ┌────────────┐          │   │
│  │  │   Skill    │ │   GitHub   │ │  Ranking   │          │   │
│  │  │  Service   │ │  Service   │ │  Service   │          │   │
│  │  └────────────┘ └────────────┘ └────────────┘          │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        DATA LAYER                               │
│                                                                 │
│  ┌────────────────────┐     ┌────────────────────┐             │
│  │    PostgreSQL      │     │      Redis         │             │
│  │                    │     │                    │             │
│  │  • skills          │     │  • rankings cache  │             │
│  │  • users           │     │  • session         │             │
│  │  • ratings         │     │  • rate limit      │             │
│  │  • tags            │     │                    │             │
│  └────────────────────┘     └────────────────────┘             │
│                                                                 │
│         Railway (MVP)  →  AWS RDS/ElastiCache (Scale)          │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      EXTERNAL                                   │
│                                                                 │
│  ┌────────────────────┐                                        │
│  │    GitHub API      │                                        │
│  │                    │                                        │
│  │  • Fetch skill.md  │                                        │
│  │  • Parse frontmatter│                                       │
│  │  • OAuth           │                                        │
│  └────────────────────┘                                        │
└─────────────────────────────────────────────────────────────────┘
```

---

## 페이지 구조

```
/                       → 홈 (스킬 스토어)
├── /skills             → 스킬 목록 (검색, 필터)
├── /skills/[id]        → 스킬 상세
├── /skills/register    → 스킬 등록
├── /rankings           → 랭킹 리더보드
├── /auth/login         → GitHub 로그인
└── /auth/callback      → OAuth 콜백
```

---

## 데이터 모델

### ERD

```
┌──────────────┐       ┌──────────────┐
│    users     │       │    skills    │
├──────────────┤       ├──────────────┤
│ id (PK)      │       │ id (PK)      │
│ github_id    │──┐    │ author_id(FK)│──┐
│ username     │  │    │ name         │  │
│ avatar_url   │  │    │ description  │  │
│ created_at   │  │    │ version      │  │
└──────────────┘  │    │ github_url   │  │
                  │    │ location_*   │  │
                  │    │ runtime_*    │  │
                  │    │ status       │  │
                  │    │ created_at   │  │
                  │    │ updated_at   │  │
                  │    └──────────────┘  │
                  │           │          │
                  │           │          │
                  ▼           ▼          │
           ┌──────────────────────┐      │
           │       ratings        │      │
           ├──────────────────────┤      │
           │ id (PK)              │      │
           │ skill_id (FK)        │◄─────┘
           │ user_id (FK)         │◄──────┐
           │ score (1-5)          │       │
           │ comment              │       │
           │ created_at           │       │
           └──────────────────────┘       │
                                          │
┌──────────────┐       ┌──────────────┐   │
│    tags      │       │  skill_tags  │   │
├──────────────┤       ├──────────────┤   │
│ id (PK)      │◄──────│ tag_id (FK)  │   │
│ name         │       │ skill_id(FK) │───┘
│ count        │       └──────────────┘
└──────────────┘
                       ┌──────────────┐
                       │   rankings   │
                       ├──────────────┤
                       │ skill_id(FK) │
                       │ elo_score    │
                       │ rank         │
                       │ usage_count  │
                       │ avg_rating   │
                       │ updated_at   │
                       └──────────────┘
```

### 테이블 정의

```sql
-- Users (GitHub OAuth)
CREATE TABLE users (
  id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  github_id     INTEGER UNIQUE NOT NULL,
  username      VARCHAR(100) NOT NULL,
  avatar_url    TEXT,
  created_at    TIMESTAMPTZ DEFAULT NOW()
);

-- Skills
CREATE TABLE skills (
  id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  author_id     UUID REFERENCES users(id),
  name          VARCHAR(200) NOT NULL,
  description   TEXT NOT NULL,
  version       VARCHAR(20) NOT NULL,
  github_url    TEXT NOT NULL,
  
  -- Location (Layer 2)
  location_type VARCHAR(20) DEFAULT 'github',
  location_owner VARCHAR(100),
  location_repo VARCHAR(100),
  location_path TEXT,
  location_ref  VARCHAR(100),
  
  -- Runtime (Layer 2)
  runtime_claude_code VARCHAR(20),
  runtime_min_model   VARCHAR(20),
  runtime_tools       JSONB,
  
  -- Resources (Layer 2)
  base_tokens   INTEGER,
  context_hint  VARCHAR(20),
  
  -- Extended (Layer 3)
  use_cases     JSONB,
  target_roles  JSONB,
  license       VARCHAR(50),
  status        VARCHAR(20) DEFAULT 'active',
  icon          VARCHAR(50),
  
  created_at    TIMESTAMPTZ DEFAULT NOW(),
  updated_at    TIMESTAMPTZ DEFAULT NOW()
);

-- Tags
CREATE TABLE tags (
  id    UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name  VARCHAR(50) UNIQUE NOT NULL,
  count INTEGER DEFAULT 0
);

-- Skill-Tag relation
CREATE TABLE skill_tags (
  skill_id UUID REFERENCES skills(id) ON DELETE CASCADE,
  tag_id   UUID REFERENCES tags(id) ON DELETE CASCADE,
  PRIMARY KEY (skill_id, tag_id)
);

-- Ratings
CREATE TABLE ratings (
  id         UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  skill_id   UUID REFERENCES skills(id) ON DELETE CASCADE,
  user_id    UUID REFERENCES users(id) ON DELETE CASCADE,
  score      INTEGER CHECK (score >= 1 AND score <= 5),
  comment    TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(skill_id, user_id)
);

-- Rankings (computed, cached)
CREATE TABLE rankings (
  skill_id    UUID PRIMARY KEY REFERENCES skills(id) ON DELETE CASCADE,
  elo_score   INTEGER DEFAULT 1000,
  rank        INTEGER,
  usage_count INTEGER DEFAULT 0,
  avg_rating  DECIMAL(3,2),
  updated_at  TIMESTAMPTZ DEFAULT NOW()
);
```

---

## API 설계

### Endpoints

| Method | Path | Description |
|--------|------|-------------|
| GET | /api/skills | 스킬 목록 (페이지네이션, 필터) |
| GET | /api/skills/:id | 스킬 상세 |
| POST | /api/skills | 스킬 등록 (auth required) |
| PUT | /api/skills/:id | 스킬 수정 (author only) |
| DELETE | /api/skills/:id | 스킬 삭제 (author only) |
| POST | /api/skills/:id/rate | 평점 등록 (auth required) |
| GET | /api/rankings | 랭킹 목록 |
| GET | /api/tags | 태그 목록 |
| GET | /api/auth/login | GitHub OAuth 시작 |
| GET | /api/auth/callback | OAuth 콜백 |
| GET | /api/auth/me | 현재 유저 정보 |
| POST | /api/auth/logout | 로그아웃 |

### Request/Response 예시

```typescript
// GET /api/skills?page=1&limit=20&tag=planning
{
  "data": [
    {
      "id": "uuid",
      "name": "prd-writer",
      "description": "PRD 작성 스킬",
      "version": "1.0.0",
      "author": {
        "username": "woosgem",
        "avatar_url": "..."
      },
      "tags": ["planning", "prd"],
      "icon": "📝",
      "stats": {
        "avg_rating": 4.5,
        "usage_count": 123,
        "rank": 3
      }
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 20,
    "total": 156,
    "total_pages": 8
  }
}
```

---

## 기술 스택 상세

### Frontend

| 기술 | 용도 |
|------|------|
| Next.js 14 | App Router, SSR |
| TypeScript | 타입 안정성 |
| @woosgem/ds-react | UI 컴포넌트 |
| @woosgem/ds-styles | 테마/스타일 |
| TanStack Query | 서버 상태 관리 |
| Zustand | 클라이언트 상태 |

### Backend

| 기술 | 용도 |
|------|------|
| Next.js API Routes | API 서버 |
| Prisma | ORM |
| Zod | 유효성 검사 |
| NextAuth.js | 인증 (GitHub OAuth) |

### Infrastructure

| 서비스 | 용도 | 환경 |
|--------|------|------|
| Vercel | 호스팅 | MVP |
| Railway (PostgreSQL) | 메인 DB | MVP |
| Railway (Redis) | 캐시 | MVP |
| AWS | 호스팅 | Scale |
| GitHub API | 스킬 소스 | All |

---

## 디렉토리 구조

```
skillhub/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── layout.tsx
│   │   ├── page.tsx            # 홈
│   │   ├── skills/
│   │   │   ├── page.tsx        # 목록
│   │   │   ├── [id]/
│   │   │   │   └── page.tsx    # 상세
│   │   │   └── register/
│   │   │       └── page.tsx    # 등록
│   │   ├── rankings/
│   │   │   └── page.tsx
│   │   └── api/
│   │       ├── skills/
│   │       ├── rankings/
│   │       └── auth/
│   │
│   ├── components/             # UI 컴포넌트
│   │   ├── layout/
│   │   ├── skill/
│   │   └── common/
│   │
│   ├── lib/                    # 유틸리티
│   │   ├── prisma.ts
│   │   ├── github.ts
│   │   └── auth.ts
│   │
│   ├── services/               # 비즈니스 로직
│   │   ├── skill.service.ts
│   │   ├── github.service.ts
│   │   └── ranking.service.ts
│   │
│   └── types/                  # TypeScript 타입
│       ├── skill.ts
│       └── api.ts
│
├── prisma/
│   └── schema.prisma
│
├── public/
├── docs/
├── package.json
└── ...
```

---

## MVP 범위

### Phase 1: 기본 스토어 (Week 1-2)

- [ ] 프로젝트 셋업 (Next.js, Prisma, 인증)
- [ ] 스킬 목록 페이지
- [ ] 스킬 상세 페이지
- [ ] 검색/필터 기능
- [ ] 태그 시스템

### Phase 2: 등록/인증 (Week 3)

- [ ] GitHub OAuth
- [ ] 스킬 등록 (GitHub URL → frontmatter 파싱)
- [ ] 내 스킬 관리

### Phase 3: 평점/랭킹 (Week 4)

- [ ] 평점 시스템
- [ ] 랭킹 계산
- [ ] 리더보드 페이지

### Phase 4: 폴리싱 (Week 5)

- [ ] 반응형? (데스크탑 전용이라 스킵)
- [ ] 에러 처리
- [ ] 성능 최적화
- [ ] 배포

---

## 리스크 & 대응

| 리스크 | 대응 |
|--------|------|
| GitHub API rate limit | 캐싱, 인증된 요청 사용 |
| 악성 스킬 등록 | 신고 시스템, 수동 검토 (나중에 자동화) |
| 스케일링 | MVP는 작게 시작, 필요시 AWS 이전 |
| 디자인 시스템 부족 | SkillHub 만들면서 DS 확장 |

---

*Architecture by Thrall ⚡*
*Reviewed by Woosgem*
