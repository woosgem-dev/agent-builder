# Agent Builder - Design Document

> Created: 2026-02-09

## 1. Overview

Claude Code 에이전트를 GUI + AI 대화로 조립하는 웹앱.
skills.sh의 스킬을 의도 기반으로 검색하고, 에이전트에 연결한다.

### 핵심 기능

| 기능 | 설명 |
|------|------|
| **Skill Finder** | skills.sh 스킬을 의도 기반으로 검색 (이름이 아닌 description/tags 검색) |
| **Agent Builder** | GUI 폼 + AI 대화로 에이전트 .md 파일 조립, 스킬 연결 포함 |

### 단계별 목표

- **Phase 1 (MVP):** 개인 도구 - localhost에서 사용
- **Phase 2:** 퍼블릭 웹 서비스로 전환

---

## 2. 기술 스택

| 영역 | 선택 | 이유 |
|------|------|------|
| Framework | Next.js 14 (App Router) | 풀스택, API Route |
| UI | @woosgem-dev/react | 자체 디자인 시스템 |
| Styling | @woosgem-dev/styles (SCSS) | DS 토큰/테마 |
| ORM | Prisma | 타입 안전, 마이그레이션 |
| DB | SQLite | 설치 불필요, 경량. 퍼블릭 전환 시 PostgreSQL |
| AI | Claude API | 에이전트 생성 + 스킬 추천 |
| Validation | Zod | 런타임 타입 검증 |

---

## 3. 시스템 아키텍처

```
┌─────────────────────────────────────────────┐
│  Agent Builder (Next.js)                     │
├──────────────┬──────────────────────────────┤
│  Pages       │  API Routes                  │
│              │                              │
│  /           │  POST /api/chat         ← AI 대화 (Claude API)
│  /skills     │  GET  /api/skills       ← 스킬 검색
│  /build      │  POST /api/skills/sync  ← 크롤링 트리거
│              │                              │
├──────────────┴──────────────────────────────┤
│  Skill Index DB (SQLite)                     │
│  frontmatter 메타데이터 + URL만 저장          │
├─────────────────────────────────────────────┤
│  External                                    │
│  - skills.sh (크롤링 → 스킬 목록)            │
│  - GitHub (SKILL.md frontmatter fetch)       │
│  - Claude API (대화/추천)                    │
└─────────────────────────────────────────────┘
```

---

## 4. 데이터 모델

### SkillIndex

| 필드 | 타입 | 설명 |
|------|------|------|
| id | String (cuid) | PK |
| name | String | 스킬 이름 |
| description | String | 스킬 설명 |
| owner | String | GitHub owner |
| repo | String | GitHub repo |
| skillId | String | 스킬 식별자 |
| url | String | skills.sh URL |
| githubUrl | String | GitHub repo URL |
| tags | String[] | 태그 목록 |
| installs | Int | 설치 수 (skills.sh) |
| syncedAt | DateTime | 마지막 동기화 |

---

## 5. 페이지 구성

### 5.1 홈 (`/`)

서비스 소개 + Skill Finder / Agent Builder 진입점.

### 5.2 Skill Finder (`/skills`)

skills.sh 스킬을 의도 기반으로 검색하는 페이지.

```
┌──────────────────────────────────────────────┐
│  Skill Finder                                │
├──────────────────────────────────────────────┤
│  🔍 "코드 리뷰 도와주는 스킬"                  │
│     description, tags 기반 검색               │
├──────────────────────────────────────────────┤
│  결과:                                       │
│  ┌────────────────────────────────────────┐  │
│  │ code-review-checklist     📥 12.3K    │  │
│  │ "PR 코드 리뷰 체크리스트..."            │  │
│  │ [skills.sh 링크]  [에이전트에 추가 →]  │  │
│  └────────────────────────────────────────┘  │
└──────────────────────────────────────────────┘
```

- skills.sh는 이름 검색만 가능 → 우리는 description + tags 기반 검색
- "에이전트에 추가" 버튼으로 빌더와 연결

### 5.3 Agent Builder (`/build`)

GUI 폼 + AI 대화로 에이전트 .md 파일을 조립하는 메인 페이지.

```
┌──────────────────────────────────────────────┐
│  Agent Builder                               │
├──────────────────┬───────────────────────────┤
│                  │                           │
│  [AI 대화]       │  [설정 폼]                │
│                  │                           │
│  "백엔드 시니어   │  Name: be-senior          │
│   에이전트 만들고 │  Description: ...         │
│   싶어"          │  Model: [opus ▼]          │
│                  │  Tools: [✓Read ✓Write...] │
│  AI: "어떤 기술   │                           │
│  스택을 쓰나요?" │  Skills:                  │
│                  │  [+ 스킬 추가]            │
│                  │  • brainstorming          │
│                  │  • test-driven-dev        │
│                  │                           │
├──────────────────┴───────────────────────────┤
│  [실시간 .md 프리뷰]                          │
└──────────────────────────────────────────────┘
```

- 왼쪽: AI 대화 → 에이전트 본문 생성 + 스킬 추천
- 오른쪽: 폼으로 frontmatter 설정 (AI 대화 시 자동 채움)
- 하단: 완성된 .md 실시간 미리보기

---

## 6. 에이전트 .md 출력 포맷

빌더의 결과물. Claude Code 에이전트 설정 파일.

```yaml
---
name: be-senior
description: NestJS 백엔드 시니어 개발자
model: opus
tools: [Read, Write, Edit, Bash, Glob, Grep]
skills:
  - test-driven-development
  - systematic-debugging
  - requesting-code-review
---
```

```markdown
# BE Senior

## 페르소나
...

## 기술 스택
...

## 핵심 원칙
...

## DO / DON'T
...

## 협업
...
```

### Frontmatter 필드

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| name | string | O | 에이전트 식별자 (lowercase, hyphens) |
| description | string | O | 에이전트 설명 |
| model | string | - | sonnet / opus / haiku / inherit |
| tools | string[] | - | 사용 가능한 도구 |
| disallowedTools | string[] | - | 차단할 도구 |
| skills | string[] | - | 시작 시 로드할 스킬 |
| maxTurns | number | - | 최대 턴 수 |
| permissionMode | string | - | 권한 모드 |
| mcpServers | object | - | MCP 서버 설정 |

### 내보내기

- 클립보드 복사
- .md 파일 다운로드

---

## 7. AI 대화 시스템

### 역할 1: 에이전트 설정 생성

사용자의 자연어 입력을 분석하여:
- frontmatter 필드 자동 채움 (name, description, model, tools)
- 본문 생성 (페르소나, 기술 스택, 원칙, DO/DON'T, 협업)

시스템 프롬프트에 에이전트 .md 포맷 + 기존 에이전트 예시를 포함.

### 역할 2: 스킬 추천

- 스킬 인덱스 DB에서 에이전트 역할에 맞는 후보 검색
- Claude가 컨텍스트 기반으로 최종 선별/추천
- 사용자가 수락하면 skills 필드에 추가

### Claude API 호출 구조

```
POST /api/chat
  → 시스템 프롬프트: 에이전트 .md 포맷 + 예시
  → 스킬 추천 시: DB에서 후보 검색 → 컨텍스트에 포함
  → 응답: 폼 업데이트 + 대화 메시지
```

---

## 8. 크롤링 파이프라인

```
[1단계] skills.sh 크롤링
  → 스킬 목록 수집 (name, owner/repo, installs, url)

[2단계] GitHub fetch
  → 각 스킬의 SKILL.md raw 파일 요청
  → frontmatter 파싱 (name, description, tags 등)

[3단계] DB 저장
  → SkillIndex에 upsert (syncedAt 갱신)
```

### 실행 방식

- MVP: `POST /api/skills/sync` 수동 트리거
- 나중에: cron job 주기적 동기화 (하루 1회)

### 크롤링 범위

- 처음: skills.sh 리더보드 상위 스킬 (인기순)
- 점진적 확대

---

## 9. MVP 범위

### 포함

- [x] Skill Finder (의도 기반 스킬 검색)
- [x] Agent Builder (GUI 폼 + AI 대화)
- [x] 스킬 연결 (skills frontmatter)
- [x] .md 파일 내보내기 (복사/다운로드)
- [x] skills.sh 크롤러 (수동 트리거)

### 제외 (MVP 이후)

- [ ] 사용자 인증
- [ ] 에이전트 저장/관리
- [ ] 에이전트 공유/커뮤니티
- [ ] 자동 크롤링 스케줄
- [ ] 스킬 품질 점수
